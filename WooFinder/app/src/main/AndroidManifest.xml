<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.woofinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WooFinder">
        <activity
            android:name=".AnimalActivity"
            android:exported="false"
            android:label="@string/title_activity_animal"
            android:theme="@style/Theme.WooFinder.NoActionBar" />
        <activity
            android:name=".SolicitudActivity"
            android:exported="false" />
        <activity
            android:name=".PruebaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>